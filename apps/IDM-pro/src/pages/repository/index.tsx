import { RepositoryComponent } from '@core/ui/components';
import { Box } from '@mui/material';

export default function RepositoryPage() {
  return (
    <Box>
      <RepositoryComponent />
    </Box>
  );
}
