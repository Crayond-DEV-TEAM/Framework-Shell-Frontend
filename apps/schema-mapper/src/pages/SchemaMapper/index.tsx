import { Feature, SchemaMapper, SchemaLoader } from '@core/ui/components';
import { Box } from '@mui/material';
export default function SchemaMapperPage() {
  return (
    <Box>
      {/* <SchemaMapper /> */}
      <SchemaLoader />
    </Box>
  );
}
