import { Organisation, Service, SideBarIdm, UserProfile } from '@core/ui/components';
import { Box } from '@mui/material';

export default function OrganisationPage() {
  return (
    <Box>
      <Organisation />
    </Box>
  );
}
